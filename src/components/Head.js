import React, { useEffect, useState } from "react";
import PropTypes from "prop-types";

import "./Head.css";

export default function HeadSection(props) {
  return (
    <div className="container head-section">
        <div className="head-section-title">Photogrammetry Hub</div>
        <div className="sep-dark" />
    </div>
  );
}
